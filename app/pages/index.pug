include ../blocks/head/head
include ../blocks/block-main/block-main
include ../blocks/block-result/block-result
include ../blocks/block-difference/block-difference
include ../blocks/block-lessons/block-lessons
include ../blocks/block-worth/block-worth
include ../blocks/block-buy/block-buy
include ../blocks/block-access/block-access
include ../blocks/footer/footer
include ../blocks/popups/popups

doctype html
html(lang='ru')
  +head({
    title: 'Предсказательный феншуй 9 дворцов | ЛЮМОС22',
    keywords: '',
    description: ''
  })
  body
    // main
    +main({
    })

    // result
    +result({
    })

    // difference
    +difference({
    })

    // lessons
    +lessons({
    })

    // worth
    +worth({
    })

    // buy
    +buy({
    })

    // access
    +access({
    })

    // footer
    +footer({
    })

    // popups
    +popup({
      class: 'popupCall',
      title: 'Оставить заявку'
    })

    // scripts
    script(src='../js/jquery-3.4.1.min.js')
    script(src='../js/jquery.mask.min.js')
    script(src='../js/wow.js')
    script(src='../js/jquery.bpopup.min.js')
    script(src='../js/scripts.js')